# Secrets — never commit
.env
local.properties
*.keystore
*.jks
!debug.keystore
# Backups de archivos de secretos (.env.bak, local.properties.bak, ...)
*.bak

# Gradle / build
.gradle/
build/
captures/
.cxx/
*.apk
*.aab

# IDE
.idea/
*.iml
.vscode/

# OS
.DS_Store

# Scratch
*.log
scripts/out/
# Qué bares ya se consultaron contra Places: es caché de gasto, no fuente de
# verdad, y depende de la base de cada quien. Ver scripts/link_place_ids.mjs.
scripts/.place-ids.checkpoint.json

# Artefactos de build de IDEs (plugin de Kotlin en VS Code)
bin/

# Claves del frontend web
web/.env.local
web/node_modules/
web/dist/

# Dump de datos para migrar a Neon: trae emails y google_sub de usuarios
birrapp-data.sql
